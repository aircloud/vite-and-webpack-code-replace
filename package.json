{
  "private": true,
  "name": "vite-and-webpack-code-replace",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm -r build",
    "lint": "eslint --ext .js,.ts",
    "release": "pnpm run build && lerna publish"
  },
  "keywords": ["vite", "webpack", "loader", "plugin"],
  "author": "aircloud",
  "license": "MIT",
  "dependencies": {
    "@lerna-lite/cli": "^1.15.1",
    "@typescript-eslint/eslint-plugin": "^5.28.0",
    "@typescript-eslint/parser": "^5.28.0",
    "eslint": "8.15.0"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
